# AGENTS.md

## 1. このリポジトリの目的
- このリポジトリは、**GitHub Pages向けの静的サイト**として「共通テスト・センター試験 自動採点サイト」を公開・運用するためのものです。
- 問題文本文や解説本文を転載することは目的ではなく、正解番号・配点・関連リンクを用いた採点支援を主目的とします。

## 2. 主要ファイルの説明
- `index.html`
  - サイト本体（UI、スタイル、クライアントサイドJavaScriptを含む）。
  - 採点処理・表示処理の中心。
- `answer_keys_verified.json`
  - 年度・科目・試験種別ごとの正解データおよび配点データ本体。
- `statistics_final.json`
  - 正答率・平均点などの統計データ。
- `source_index.json`
  - 同梱データの索引・メタ情報。
- `README.md` / `README_GITHUB_PAGES.txt`
  - GitHub Pages公開に関する前提・更新メモ。
- `README_NETLIFY.txt` / `_headers`
  - Netlify向け運用情報。**明示依頼がない限り触らない。**
- `robots.txt` / `nojekyll`
  - クローラ制御やGitHub Pages公開補助に関するファイル。

## 3. データ追加時の注意
- `answer_keys_verified.json` の解答データは、**推測で追加・修正しない**。
- 追加・修正の根拠（一次情報や既存運用ルール）がない変更は行わない。
- 既存の年度（`year`）・科目（`subject`）・試験種別（`exam`）のデータ形式を壊さない。
- 既存キー構造・型・命名（例: `questions`, `maxScore`, `sourceUrl` など）との互換性を維持する。
- 一部データだけを更新する場合も、JSON全体の整合性を確認する。

## 4. 採点ロジック変更時の注意
- 採点ロジック（正誤判定、部分点、選択問題処理など）は、**明示的な依頼がある場合のみ変更する**。
- 変更時は、仕様変更の意図・影響範囲（既存年度/科目への影響）を明確にし、後方互換性を確認する。
- `index.html` 内の採点関連関数（例: `match`, `grade` など）を変更する場合、関連表示や集計項目との整合性を必ず確認する。

## 5. UI変更時の注意
- このサイトは静的配信前提のため、UI変更は `index.html` を中心に最小差分で行う。
- モバイル表示・スクロール・操作性（キーパッド、入力欄、結果表）を壊さない。
- 文言変更時は、利用条件・注意文・権利関係の既存方針と矛盾させない。
- 明示依頼がない限り、デザインの大規模刷新や情報構造変更は避ける。

## 6. GitHub Pages公開時の注意
- 本リポジトリは **GitHub Pages向け** を正とする。
- `nojekyll`、`robots.txt`、`index.html` 内メタ（`noindex,nofollow`）など公開前提の挙動を不用意に変更しない。
- **Netlify向けの出力や設定変更は、明示的に依頼されない限り行わない。**
- 公開手順や配置前提（リポジトリ直下に必要ファイルを置く運用）を崩さない。
- リリース作業・公開用ZIP作成・更新版作成を行う場合は、既存のプロジェクト慣例に従って、バージョン番号と最終更新日時を必ず確認・更新する。

## 7. 作業後の報告ルール
- 作業後は必ず以下を報告する。
  1. 変更したファイル一覧
  2. 変更内容の要約（何を、なぜ変更したか）
  3. 互換性への影響有無（データ形式・採点ロジック・公開設定）
  4. 必要に応じて確認手順（表示確認・JSON整合性確認など）
- 依頼範囲外の変更を行っていないことを明記する。
